

object MergeSort extends App{

 /* def mergeSort(l:Array[Int],start:Int,last:Int):Array[Int] = {
    if (l.size>1){
      val mid = l.size/2
      val left = l.slice(0,mid)
      val right = l.slice(mid,last)

      val l1:Array[Int] = mergeSort(left,start,mid)
      val l2:Array[Int] = mergeSort(right,mid,last)

      var i = 0
      var j = 0
      var temp = 0
      while (i<l1.size && j<l2.size){
        if (l2(j)<l1(i)){
          
          j+=1

        }
      }


    }

  } */

}
